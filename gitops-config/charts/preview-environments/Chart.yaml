apiVersion: v2
name: preview-environments
description: Feature branch preview environments using ArgoCD Pull Request Generator
type: application
version: 1.0.0
appVersion: "1.0.0"

# Dependencies: Requires platform-core for Vault role
# Requires ingress-cloudflare for Gateway listener
