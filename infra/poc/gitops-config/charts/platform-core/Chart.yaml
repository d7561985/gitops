apiVersion: v2
name: platform-core
description: Core platform infrastructure - namespaces, gateway, applicationset, vault
type: application
version: 1.0.0
appVersion: "1.0.0"

# Core platform components:
# - Environment namespaces (poc-dev, poc-staging, etc.)
# - Gateway per environment with domain routing
# - ApplicationSet for services deployment
# - Domain mirrors (additional domains for same app)
# - VaultAuth resources
# - Bootstrap job for Vault policies
